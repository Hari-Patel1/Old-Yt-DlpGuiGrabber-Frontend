import 'package:flutter/material.dart';

class AutoTagPage extends StatelessWidget {
  const AutoTagPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Auto Tag Page"),
      ),
    );
  }
}
